# Completion checks
- Run `node --test` and inspect pass/fail counts.
- Parse changed JSON files with Node.
- Check POWER.md metadata/onboarding claims against implementation and grep evidence.
- Sanity-check memory references with `serena memories check` from project root.