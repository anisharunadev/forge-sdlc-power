# Commands
- Run tests with `node --test` (or package.json test script if defined).
- Validate JSON via `node -e "JSON.parse(require('fs').readFileSync(...))"`.
- Inspect evidence with `grep -n` and `git diff`/`git status`.